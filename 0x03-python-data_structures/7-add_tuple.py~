#!/usr/bin/python3
def add tuple(tuple_a=(), tuple_b=()):
    l1 = len(tuple_a)
    l2 = len(tuple_b)
    if(l1 >= 2 and l2 >= 2):
        sum_t = (tuple_a[0] + tuple_b[0], tuple_a[1] +tuple_b[1])
    elif(l1 == 1 and l2 >= 2):
        sum_t = (tuple_a[0] + tuple_b[0], tuple_b[1])
    elif(l1 == 0 and l2 >= 2):
